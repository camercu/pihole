{# Managed by Ansible (roles/backup). Mode 0600 — holds secrets. #}
RESTIC_REPOSITORY={{ backup_restic_repository }}
RESTIC_PASSWORD={{ backup_restic_password }}
PIHOLE_PASSWORD={{ pihole_web_password }}
PIHOLE_API=http://localhost/api
RESTIC_KEEP_WEEKLY={{ backup_restic_keep_weekly }}

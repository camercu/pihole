---
# Weekly Pi-hole backup: Teleporter export -> restic on the NAS over SFTP.
# Entirely gated on backup_enabled so the playbook stays green until the NAS is
# reachable and the restic password is vaulted (see README "Backups").
- name: Backup role
  when: backup_enabled | bool
  block:
    - name: Install restic
      ansible.builtin.apt:
        name: restic
        state: present

    - name: Create backup config directory
      ansible.builtin.file:
        path: /etc/pihole-backup
        state: directory
        owner: root
        group: root
        mode: "0700"

    - name: Deploy backup environment file
      ansible.builtin.template:
        src: backup-env.j2
        dest: /etc/pihole-backup/env
        owner: root
        group: root
        mode: "0600"
      no_log: true # contains restic + Pi-hole passwords

    - name: Deploy backup script
      ansible.builtin.copy:
        src: pihole_backup.py
        dest: /usr/local/bin/pihole-backup.py
        owner: root
        group: root
        mode: "0755"

    # restic reads the password from RESTIC_PASSWORD_FILE natively. Passing it as
    # RESTIC_PASSWORD inline forced no_log on the two tasks below, censoring the
    # repo-init errors (unreachable NAS, wrong password) they exist to surface.
    # The repository URL is not secret, so it stays inline; only the password
    # moves to a root-only file, letting these tasks run without no_log.
    - name: Write restic password to a root-only file
      ansible.builtin.copy:
        content: "{{ backup_restic_password }}"
        dest: /etc/pihole-backup/restic-password
        owner: root
        group: root
        mode: "0600"
      no_log: true # the password itself

    - name: Initialise restic repository if absent
      ansible.builtin.command:
        cmd: restic cat config
      environment:
        RESTIC_REPOSITORY: "{{ backup_restic_repository }}"
        RESTIC_PASSWORD_FILE: /etc/pihole-backup/restic-password
      register: backup_restic_check
      changed_when: false
      failed_when: false

    - name: Create restic repository
      ansible.builtin.command:
        cmd: restic init
      environment:
        RESTIC_REPOSITORY: "{{ backup_restic_repository }}"
        RESTIC_PASSWORD_FILE: /etc/pihole-backup/restic-password
      when: backup_restic_check.rc != 0
      changed_when: true

    - name: Deploy backup service and timer
      ansible.builtin.template:
        src: "{{ item }}"
        dest: "/etc/systemd/system/{{ item | replace('.j2', '') }}"
        owner: root
        group: root
        mode: "0644"
      loop:
        - pihole-backup.service.j2
        - pihole-backup.timer.j2

    - name: Enable and start the weekly backup timer
      ansible.builtin.systemd_service:
        name: pihole-backup.timer
        enabled: true
        state: started
        daemon_reload: true

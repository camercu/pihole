{# Managed by Ansible (roles/common). Edit lan_hosts in group_vars/all.yml. #}
127.0.0.1       localhost
::1             localhost ip6-localhost ip6-loopback
ff02::1         ip6-allnodes
ff02::2         ip6-allrouters

{% for host in lan_hosts %}
{{ "%-15s" | format(host.ip) }} {{ host.names | join(' ') }}
{% endfor %}

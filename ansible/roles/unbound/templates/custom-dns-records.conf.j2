{# Managed by Ansible (roles/unbound). Derived from lan_hosts (group_vars). #}
server:
{% for host in lan_hosts %}
{% for name in host.names %}
  local-data: "{{ name }} A {{ host.ip }}"
{% endfor %}
{% endfor %}

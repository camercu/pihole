# Ansible runtime artifacts
.ansible/
*.retry
ansible/collections/

# uv / python (setup.sh toolchain)
.venv/
__pycache__/

# Vault passphrase file — NEVER commit
.vault-pass
ansible/.vault-pass

# Per-user config, generated by ./setup.sh — each person has their own
ansible/group_vars/all/vault.yml
ansible/group_vars/all/local.yml

# Claude
.claude/

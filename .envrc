use nix

# Auto-load the Ansible vault passphrase when present (gitignored file).
# Absolute path so it works regardless of the dir ansible-playbook runs from.
# No file => no export, so runs without secrets still work.
if [ -f ansible/.vault-pass ]; then
  export ANSIBLE_VAULT_PASSWORD_FILE="$PWD/ansible/.vault-pass"
fi
